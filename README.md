Get a list off all devices with their IP addresses from a [jNetMap](http://www.rakudave.ch/jnetmap/) file (`.jnm`).

## Installation

    pip install -r requirements.txt

## Run

    ./ip-list.py -f jnetmapfile.jnm
